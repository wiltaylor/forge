{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Forge demo main window: core defaults plus the full Forge IPC surface.",
  "windows": ["main"],
  "permissions": ["core:default", "forge:default"]
}
