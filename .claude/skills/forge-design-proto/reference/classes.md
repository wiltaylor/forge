# SolidJS class vocabulary

<!-- PROTOTYPE STUB (wayfinder #64). Did not exist when the routing probes ran. Probe 2
     invented `.fset`, `.fset-section` and `.fset-row` because the class names were only
     documented inside two filled implementation pages. -->

Every Forge class name, and which control owns it.

Not written.
