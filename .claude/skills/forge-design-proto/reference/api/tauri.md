# Tauri — transport and packaging

<!-- PROTOTYPE STUB (wayfinder #64). -->

Tauri is not a rendering platform. There is no Tauri column in the control grid, because
a Tauri app renders through SolidJS.

What is Tauri-specific: IPC in place of HTTP, the command and event surface, the desktop
shell and window, and packaging.

Not written.

For control work inside a Tauri app, follow `SKILL.md` and use the `solid` implementation
pages.
