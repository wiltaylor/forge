# Backend

<!-- PROTOTYPE STUB (wayfinder #64). The shape of the backend material inside forge-design
     is still fog on the map. This file exists so the job triage in SKILL.md has a real
     target and the "what do I not read" route can be tested. -->

The doc store, actions, events and SSE, and how to rebuild them on axum or FastAPI against
the frozen contract.

Not written.

Control pages do not apply to backend work. Do not read them.

- `axum.md` — the Rust rendering
- `fastapi.md` — the Python rendering
- `client.md` — the TypeScript client
- `contract.md` — the normative wire contract
