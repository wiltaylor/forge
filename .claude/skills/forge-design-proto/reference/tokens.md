# Tokens

<!-- PROTOTYPE STUB (wayfinder #64). This page did not exist when the routing probes ran.
     Every probe that needed a value invented one: probe 2 minted `--f-*` variable names
     from the role table in laws.md, and probe 1 invented a Theme accessor for egui.
     That is the exact failure anti-patterns.md calls "naming what does not exist".
     The real page is a new ticket. -->

The spelling of every role, on every platform. `laws.md` names the roles; this page gives
the names you actually type.

Not written.
