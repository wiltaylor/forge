[package]
name = "forge-contract"
description = "The Forge contract corpus (contract/corpus.json) plus the loader, validator and matcher its Rust drivers share. Transport-free."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
