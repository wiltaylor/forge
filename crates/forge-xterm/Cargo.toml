[package]
name = "forge-xterm"
description = "The xterm wire encodings shared by the Forge kits: key sequences, mouse reports and their mode gating."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# No dependencies, on purpose. forge-tui has no async runtime and does not
# want one, so the encodings it shares with forge-egui must stay sync and
# dependency-free. Keep this section empty.
[dependencies]
